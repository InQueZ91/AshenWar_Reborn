info:
  name: GetAllUnitSummary
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/api/definitions/units"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
